# ============================================
# APPLICATION SETTINGS
# ============================================
spring.application.name=thesis
server.port=8080
app.base-url=http://localhost:8080
app.name=Thesis Cloud Storage
app.version=1.0.0

# ============================================
# DATABASE (PostgreSQL)
# ============================================
spring.datasource.url=jdbc:postgresql://localhost:5432/thesis_db
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool (HikariCP)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.generate_statistics=false

# ============================================
# JWT SECURITY (??????????? ???? ????!)
# ============================================
# ????????? ?????: openssl rand -base64 64
# ??? ??????????? ???? ??? ?????? (?? ???????? ????? ??????? ???????):
app.jwt.secret=ndOcfNslRaxdjCPzxCkImz0Us9B5rCfUgQYECx0lc436NjNokOsXQotf+e2QuO84rb3Ms5w4hDDiJihGYrKXrQ==
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000
app.jwt.issuer=thesis-app
app.jwt.audience=thesis-client

# ============================================
# EMAIL CONFIGURATION (Yandex Mail)
# ============================================

# ???????? ????????? ???????
spring.mail.host=smtp.yandex.com
spring.mail.port=465
spring.mail.protocol=smtps
spring.mail.username=Cloud-Storage-Assistent@yandex.ru
spring.mail.password=snrhurneizevxjcc

# ????????? SMTP
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true

# ??????????? (?????? ????????? ? ???????)
spring.mail.from=Cloud-Storage-Assistent@yandex.ru

# ???????? (???????????)
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ??????? (???????? ???????? ??? ????????)
mail.debug=true

# ============================================
# FILE UPLOAD SETTINGS
# ============================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.file-size-threshold=2MB
spring.servlet.multipart.location=./temp

# ?????????? ??? ???????? ??????????? ??????
file.upload.dir=./uploads
file.max-size-per-user=1073741824
file.max-size-per-group=5368709120

# ============================================
# SECURITY & BEAN CONFIGURATION
# ============================================
spring.main.allow-bean-definition-overriding=true
spring.main.banner-mode=console

# ============================================
# ASYNC / SCHEDULING
# ============================================
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100
spring.task.execution.thread-name-prefix=async-
spring.task.execution.shutdown.await-termination=true
spring.task.execution.shutdown.await-termination-period=30s

# ============================================
# WEB / SERVLET
# ============================================
server.servlet.context-path=/
server.servlet.session.timeout=30m
server.servlet.session.tracking-modes=cookie
server.error.include-stacktrace=never
server.error.include-message=always
server.error.include-binding-errors=always
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json

# ============================================
# LOGGING
# ============================================
logging.level.root=INFO
logging.level.com.example.thesis=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.jdbc.core=INFO

# ???????? ???????
logging.file.name=logs/thesis-app.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.console=%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ============================================
# THYMELEAF (??? email ????????)
# ============================================
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false
spring.thymeleaf.content-type=text/html

# ============================================
# APPLICATION-SPECIFIC SETTINGS
# ============================================
# ???????????? ?????????? ?????????? ? ??????
app.group.max-members=50

# ???????????? ?????????? ????? ? ????????????
app.user.max-groups=20

# ????????? ????
app.chat.message-history-days=30
app.chat.max-message-length=1000

# ????????? ???????????
app.notifications.retention-days=90
app.notifications.batch-size=50

# ============================================
# CORS (??? ????????? ?? React)
# ============================================
# app.cors.allowed-origins=http://localhost:3000
# app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
# app.cors.allowed-headers=Authorization,Content-Type,X-Requested-With,Accept
# app.cors.exposed-headers=Authorization
# app.cors.allow-credentials=true
# app.cors.max-age=3600

# ============================================
# SWAGGER / OPENAPI (???? ???????? ???????????)
# ============================================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.disable-swagger-default-url=true

server.ssl.enabled=false

# Security headers
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=strict


app.frontend.url=http://localhost:3000